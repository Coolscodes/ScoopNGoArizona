# Scoop N Go HQ

Jobber-style operations hub for Scoop N Go Arizona. Next.js 14 + Supabase + Stripe.
Deployed as its own Vercel project (Root Directory: `hq`), separate from the
marketing site. See `SETUP.md` for environment + migration steps and
`../OPS_HUB_PLAN.md` for the full build plan.
