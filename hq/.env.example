# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://emvqtgsjdbyaionxguhq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=            # server-only, never exposed to the browser

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Email (Resend)
RESEND_API_KEY=

# Cron protection
CRON_SECRET=

# SMS (Twilio) — Workstream 8
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# AI (Claude) — assistant, briefings, reports insights. Key from console.anthropic.com.
ANTHROPIC_API_KEY=

# Meta instant-form lead alerts — /api/cron/meta-leads
# Long-lived Page access token with leads_retrieval. Meta will not email or text
# you a new lead, so we poll for them and send the alert ourselves.
META_PAGE_ACCESS_TOKEN=
LEAD_ALERT_EMAIL=                # optional, defaults to jettbrown44@gmail.com
LEAD_ALERT_SMS_EMAIL=6026220238@vtext.com   # Verizon gateway, arrives as a plain text message.
                                 # AT&T is @txt.att.net, T-Mobile is @tmomail.net.
                                 # The phone gets a 155 char summary, email gets the full breakdown.

# App
NEXT_PUBLIC_BASE_URL=http://localhost:3000
